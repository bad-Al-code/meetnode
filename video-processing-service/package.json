{
  "name": "video-processing-service",
  "version": "1.0.0",
  "description": "A microservice-based video processing pipeline",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "pnpm run -r build",
    "dev": "echo \"Run dev scripts from indeividual packages or use a process manger like concurrently or npm-run-all\"",
    "lint": "pnpm run -r lint",
    "clean": "pnpm run -r clean && rm -rf node_modules",
    "clean:all": "pnpm run clean && rm -rf pnpm-lock.yaml",
    "typecheck":"pnpm run -r tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
